let
  Source = Excel.Workbook(File.Contents("\\PDC-AIP-DATA\ETS_ESSBASE_GCM_NAS_T3_PROD01\Power BI Reporting\Topline\State Cohorts.xlsx"), null, true),
  Navigation = Source{[Item = "Specialty", Kind = "Sheet"]}[Data],
  #"Promoted headers" = Table.PromoteHeaders(Navigation, [PromoteAllScalars = true]),
  #"Changed column type" = Table.TransformColumnTypes(#"Promoted headers", {{"Concat", type text}, {"LOB", type text}, {"State Abbr", type text}, {"Brand", type text}, {"2024 Color Cohort", type text}, {"2025 Color Cohort", type text}, {"Cohort Order", Int64.Type}, {"Cohort Version", type date}, {"LOB Rollup", type text}, {"Cohort Index", type text}}),
  #"Replaced value" = Table.ReplaceValue(#"Changed column type", "NA", "None", Replacer.ReplaceValue, {"2024 Color Cohort", "2025 Color Cohort"})
in
  #"Replaced value"
