let
  Source = Table.Combine({#"State Cohorts (Auto)", #"State Cohorts (Home)"}),
  #"Transform columns" = Table.TransformColumnTypes(Source, {{"Cohort Order", type text}}),
  #"Replace errors" = Table.ReplaceErrorValues(#"Transform columns", {{"Cohort Order", null}})
in
  #"Replace errors"
