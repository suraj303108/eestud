sh
pip install office365-rest-python-client
Then, you can use the following code to authenticate and read a file from SharePoint:

python
from office365.sharepoint.client_context import ClientContext
from office365.runtime.auth.user_credential import UserCredential

# SharePoint site and file details
site_url = "https://yourtenant.sharepoint.com/sites/yoursite"
file_url = "/sites/yoursite/Shared%20Documents/yourfile.txt"

# Authentication details
username = "yourusername@yourtenant.com"
password = "yourpassword"

# Authenticate and create a client context
ctx = ClientContext(site_url).with_credentials(UserCredential(username, password))

# Read the file content
file = ctx.web.get_file_by_server_relative_url(file_url).download().execute_query()
file_content = file.content

# Print the file content
print(file_content.decode(
